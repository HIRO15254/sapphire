# TASK-0504: プレイヤーCRUD基本API実装 - テストケース洗い出し

## 生成情報
- **生成日**: 2025-09-25
- **生成者**: TDD テスト設計担当者
- **タスクID**: TASK-0504
- **機能名**: プレイヤーCRUD基本API実装
- **実装対象**: get_players（ページネーション）, update_player

## 開発言語・フレームワーク

🔵 **青信号**: 既存実装調査と技術スタック定義書に基づく確実な選択

- **プログラミング言語**: Rust
  - **言語選択の理由**: Tauri バックエンドの実装言語として既に採用済み
  - **テストに適した機能**: cargo test標準フレームワーク、Result型によるエラーハンドリング、型安全性
- **テストフレームワーク**: cargo test + tokio-test
  - **フレームワーク選択の理由**: 既存プロジェクトで17件のテストが正常動作、非同期処理対応
  - **テスト実行環境**: インメモリSQLite、Databaseテストヘルパー関数活用

## テストケース一覧

### 1. 正常系テストケース（基本的な動作）

#### Test Case 1-1: get_players - デフォルトパラメータでの取得
🔵 **青信号**: 既存のget_all_players実装パターンと要件定義書のデフォルト値仕様に基づく

- **テスト名**: プレイヤー一覧取得（デフォルトパラメータ）
  - **何をテストするか**: limit, offset, sort_byすべてNoneの場合の基本動作
  - **期待される動作**: limit=50, offset=0, sort_by="name", sort_order="asc"で処理される
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: None,
      offset: None,
      sort_by: None,
      sort_order: None,
  }
  ```
  - **入力データの意味**: フロントエンドが初期表示時に送る最もシンプルなリクエスト
- **期待される結果**:
  ```rust
  GetPlayersResponse {
      players: Vec<PlayerListResponse>, // 最大50件
      total_count: i32, // 実際のプレイヤー総数
      has_more: bool, // 50件を超える場合true
  }
  ```
  - **期待結果の理由**: NFR-001（1秒以内表示）要件を満たすためのページネーション
- **テストの目的**: フロントエンドの初期表示機能の動作保証
  - **確認ポイント**: ソート順がname昇順、レスポンス構造の正確性

#### Test Case 1-2: get_players - ページネーション機能
🔵 **青信号**: api-endpoints.mdのページネーション仕様と要件定義書に基づく

- **テスト名**: プレイヤー一覧取得（ページネーション）
  - **何をテストするか**: limit/offsetを指定した場合の正しいページング動作
  - **期待される動作**: 指定されたlimit/offsetに従って正確にデータを取得する
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(10),
      offset: Some(20),
      sort_by: Some("created_at".to_string()),
      sort_order: Some("desc".to_string()),
  }
  ```
  - **入力データの意味**: 3ページ目（21-30番目）のデータを作成日時降順で取得
- **期待される結果**: 21-30番目のプレイヤーデータが作成日時降順で返される
  - **期待結果の理由**: フロントエンドでの無限スクロールやページング機能実現のため
- **テストの目的**: ページネーション機能の正確性確認
  - **確認ポイント**: オフセット位置の正確性、ソート順の正確性、has_more判定

#### Test Case 1-3: get_players - 複数ソート条件テスト
🟡 **黄信号**: api-endpoints.mdのソート仕様から各ソートフィールドでの動作を推測

- **テスト名**: プレイヤー一覧取得（ソート条件別）
  - **何をテストするか**: name, created_at, updated_at各フィールドでのソート動作
  - **期待される動作**: 指定されたフィールドで正確にソートされたデータを取得
- **入力値**:
  ```rust
  // name昇順, name降順, created_at昇順, created_at降順, updated_at昇順, updated_at降順
  GetPlayersRequest {
      sort_by: Some("updated_at".to_string()),
      sort_order: Some("desc".to_string()),
  }
  ```
  - **入力データの意味**: 最近更新されたプレイヤーから順番に取得
- **期待される結果**: updated_at降順でソートされたプレイヤー一覧
  - **期待結果の理由**: ユーザーが最近の活動を確認したい場合のUI要件
- **テストの目的**: 全ソートフィールドの動作確認
  - **確認ポイント**: 各フィールドでのソート精度、昇順・降順の正確性

#### Test Case 1-4: update_player - 名前のみ更新
🔵 **青信号**: types.rsのUpdatePlayerRequest定義と要件定義書に基づく

- **テスト名**: プレイヤー名前更新（名前のみ）
  - **何をテストするか**: プレイヤー名のみを更新し、他は変更しない動作
  - **期待される動作**: 指定されたプレイヤーの名前のみが更新される
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: Some("新しいプレイヤー名".to_string()),
      player_type_id: None, // 変更しない
  }
  ```
  - **入力データの意味**: 既存プレイヤーの名前変更（タイプ変更なし）
- **期待される結果**: 更新されたPlayerオブジェクト（名前のみ変更、updated_atが更新）
  - **期待結果の理由**: 部分更新機能により効率的なデータ変更を実現
- **テストの目的**: 部分更新機能の正確性確認
  - **確認ポイント**: 変更対象フィールドのみ更新、未指定フィールドは保持

#### Test Case 1-5: update_player - プレイヤータイプ更新
🔵 **青信号**: REQ-002（プレイヤータイプ）要件と既存実装に基づく

- **テスト名**: プレイヤータイプ更新
  - **何をテストするか**: プレイヤーのタイプIDを有効な値に変更する動作
  - **期待される動作**: 指定されたプレイヤータイプIDに正しく更新される
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: None, // 変更しない
      player_type_id: Some(Some("valid_type_id".to_string())),
  }
  ```
  - **入力データの意味**: プレイヤータイプのみを変更（名前は維持）
- **期待される結果**: player_type_idが新しい値に更新されたPlayerオブジェクト
  - **期待結果の理由**: プレイヤー分類機能の柔軟性実現
- **テストの目的**: プレイヤータイプ変更機能の動作確認
  - **確認ポイント**: 参照整合性の維持、更新の正確性

#### Test Case 1-6: update_player - 同時更新（名前+タイプ）
🟡 **黄信号**: UpdatePlayerRequest型定義から複数フィールド同時更新の動作を推測

- **テスト名**: プレイヤー情報同時更新（名前＋タイプ）
  - **何をテストするか**: 名前とプレイヤータイプを同時に更新する動作
  - **期待される動作**: 両方のフィールドが一つのトランザクションで更新される
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: Some("更新された名前".to_string()),
      player_type_id: Some(Some("new_type_id".to_string())),
  }
  ```
  - **入力データの意味**: プレイヤー情報の包括的な更新
- **期待される結果**: 名前とタイプ両方が更新されたPlayerオブジェクト
  - **期待結果の理由**: 一括更新により効率的なデータ管理を実現
- **テストの目的**: 複数フィールド同時更新の動作確認
  - **確認ポイント**: トランザクション整合性、both or neitherの保証

### 2. 異常系テストケース（エラーハンドリング）

#### Test Case 2-1: get_players - 無効なlimit値（範囲外）
🟡 **黄信号**: 一般的なページネーションAPIの制約と境界値テストから推測

- **テスト名**: 無効なlimit値でのエラーハンドリング
  - **エラーケースの概要**: limit値が範囲外（0以下、1000超過）の場合
  - **エラー処理の重要性**: DoS攻撃防止とシステム安定性確保
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(-1), // または Some(1001)
      offset: Some(0),
      sort_by: None,
      sort_order: None,
  }
  ```
  - **不正な理由**: limit値は1-1000の範囲外
  - **実際の発生シナリオ**: 悪意あるクライアントまたはフロントエンドバグ
- **期待される結果**: `Err("limitは1以上1000以下である必要があります")`
  - **エラーメッセージの内容**: ユーザーが修正方法を理解できる具体的なメッセージ
  - **システムの安全性**: 大量データ取得によるパフォーマンス劣化を防止
- **テストの目的**: 入力値バリデーションの確認
  - **品質保証の観点**: システムの堅牢性とセキュリティの向上

#### Test Case 2-2: get_players - 無効なoffset値（負数）
🟡 **黄信号**: 一般的なページネーションAPIの制約とデータベース仕様から推測

- **テスト名**: 無効なoffset値でのエラーハンドリング
  - **エラーケースの概要**: offset値が負数の場合
  - **エラー処理の重要性**: データベースエラーの防止
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(10),
      offset: Some(-5),
      sort_by: None,
      sort_order: None,
  }
  ```
  - **不正な理由**: offsetは0以上である必要がある
  - **実際の発生シナリオ**: フロントエンドの計算ミス、不正なページング操作
- **期待される結果**: `Err("offsetは0以上である必要があります")`
  - **エラーメッセージの内容**: 具体的な制約条件を明示
  - **システムの安全性**: SQLエラーの防止
- **テストの目的**: offset値バリデーションの確認
  - **品質保証の観点**: データベース操作の安全性確保

#### Test Case 2-3: get_players - 無効なsort_by値
🟡 **黄信号**: api-endpoints.mdのソートフィールド仕様と一般的なAPIエラーハンドリングから推測

- **テスト名**: 無効なソートフィールド指定エラー
  - **エラーケースの概要**: 存在しないカラム名でソートを試みる場合
  - **エラー処理の重要性**: SQLインジェクション攻撃の防止
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(10),
      offset: Some(0),
      sort_by: Some("invalid_column".to_string()),
      sort_order: Some("asc".to_string()),
  }
  ```
  - **不正な理由**: "name", "created_at", "updated_at"以外のフィールド指定
  - **実際の発生シナリオ**: フロントエンドの実装ミスまたは古いAPIクライアント
- **期待される結果**: `Err("ソートフィールドはname, created_at, updated_atのいずれかである必要があります")`
  - **エラーメッセージの内容**: 許可されたフィールド一覧を明示
  - **システムの安全性**: 不正なSQL実行の防止
- **テストの目的**: ソート機能の入力値検証
  - **品質保証の観点**: セキュリティホールの排除

#### Test Case 2-4: get_players - 無効なsort_order値
🟡 **黄信号**: 一般的なAPIの昇順・降順仕様から推測

- **テスト名**: 無効なソート順指定エラー
  - **エラーケースの概要**: "asc", "desc"以外のソート順を指定
  - **エラー処理の重要性**: 不正なSQL生成の防止
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(10),
      offset: Some(0),
      sort_by: Some("name".to_string()),
      sort_order: Some("invalid_order".to_string()),
  }
  ```
  - **不正な理由**: ソート順は"asc"または"desc"のみ有効
  - **実際の発生シナリオ**: タイポ、古いクライアント仕様
- **期待される結果**: `Err("ソート順はascまたはdescである必要があります")`
  - **エラーメッセージの内容**: 有効なオプションを明確に提示
  - **システムの安全性**: SQL構文エラーの防止
- **テストの目的**: ソート順バリデーションの確認
  - **品質保証の観点**: 入力値の厳密な検証

#### Test Case 2-5: update_player - 存在しないプレイヤーID
🔵 **青信号**: 要件定義書の存在確認要件と既存実装パターンに基づく

- **テスト名**: 存在しないプレイヤーID更新エラー
  - **エラーケースの概要**: データベースに存在しないIDでの更新試行
  - **エラー処理の重要性**: データ整合性の維持とユーザー体験の向上
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "nonexistent_player_id".to_string(),
      name: Some("新しい名前".to_string()),
      player_type_id: None,
  }
  ```
  - **不正な理由**: 指定されたIDのプレイヤーがデータベースに存在しない
  - **実際の発生シナリオ**: プレイヤー削除後の更新試行、不正なID指定
- **期待される結果**: `Err("指定されたプレイヤーが見つかりません")`
  - **エラーメッセージの内容**: ユーザーが状況を理解できる明確なメッセージ
  - **システムの安全性**: データベースの整合性維持
- **テストの目的**: 存在確認バリデーションの動作確認
  - **品質保証の観点**: ユーザー体験の向上と運用安定性

#### Test Case 2-6: update_player - プレイヤー名重複エラー
🔵 **青信号**: EDGE-001（プレイヤー名重複エラー処理）要件に明記

- **テスト名**: プレイヤー名重複更新エラー
  - **エラーケースの概要**: 既存プレイヤーと同じ名前に変更しようとする場合
  - **エラー処理の重要性**: データの一意性制約維持
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "player_1_id".to_string(),
      name: Some("既存プレイヤー名".to_string()), // 別のプレイヤーが使用中
      player_type_id: None,
  }
  ```
  - **不正な理由**: データベースの一意制約違反
  - **実際の発生シナリオ**: ユーザーが既存名を知らずに変更を試行
- **期待される結果**: `Err("同名のプレイヤーが既に存在します")`
  - **エラーメッセージの内容**: 具体的な制約違反の説明
  - **システムの安全性**: データベース制約の適切な実装
- **テストの目的**: 一意性制約の確認
  - **品質保証の観点**: データ整合性の保証

#### Test Case 2-7: update_player - 無効なプレイヤータイプID
🔵 **青信号**: 要件定義書の参照整合性要件に基づく

- **テスト名**: 存在しないプレイヤータイプID指定エラー
  - **エラーケースの概要**: データベースに存在しないplayer_type_idを指定
  - **エラー処理の重要性**: 参照整合性の維持
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: None,
      player_type_id: Some(Some("nonexistent_type_id".to_string())),
  }
  ```
  - **不正な理由**: 指定されたplayer_type_idが存在しない
  - **実際の発生シナリオ**: プレイヤータイプ削除後の更新、不正なID指定
- **期待される結果**: `Err("指定されたプレイヤータイプが存在しません")`
  - **エラーメッセージの内容**: 参照先が見つからないことを明確に表示
  - **システムの安全性**: 外部キー制約の適切な実装
- **テストの目的**: 参照整合性バリデーションの確認
  - **品質保証の観点**: データベース整合性の保証

#### Test Case 2-8: update_player - 空文字プレイヤー名
🟡 **黄信号**: 一般的なバリデーション要件と既存create_player実装から推測

- **テスト名**: 空文字プレイヤー名でのバリデーションエラー
  - **エラーケースの概要**: 名前に空文字列または空白文字のみを指定
  - **エラー処理の重要性**: 意味のあるデータの維持
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: Some("".to_string()), // または Some("   ".to_string())
      player_type_id: None,
  }
  ```
  - **不正な理由**: プレイヤー名は1文字以上の意味のある文字列が必要
  - **実際の発生シナリオ**: ユーザーの入力ミス、フォームのバリデーション不備
- **期待される結果**: `Err("プレイヤー名は必須です")`
  - **エラーメッセージの内容**: 入力が必要であることを明確に表示
  - **システムの安全性**: 無意味なデータの防止
- **テストの目的**: 名前フィールドのバリデーション確認
  - **品質保証の観点**: データ品質の維持

### 3. 境界値テストケース（最小値、最大値、null等）

#### Test Case 3-1: get_players - limit境界値（最小値1）
🟡 **黄信号**: 要件定義書のlimit範囲1-1000仕様に基づく境界値テスト

- **テスト名**: limit最小値での動作確認
  - **境界値の意味**: システムが処理可能な最小取得件数
  - **境界値での動作保証**: 最小値でも正確にページネーション動作する
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(1),
      offset: Some(0),
      sort_by: Some("name".to_string()),
      sort_order: Some("asc".to_string()),
  }
  ```
  - **境界値選択の根拠**: limit範囲の最小値1での動作確認
  - **実際の使用場面**: 詳細表示用の単一アイテム取得
- **期待される結果**: 1件のプレイヤーデータを含むレスポンス
  - **境界での正確性**: 正確に1件のみ取得、has_more判定が正確
  - **一貫した動作**: 通常のページネーションと同じ構造
- **テストの目的**: 最小limit値での動作保証
  - **堅牢性の確認**: 極端に小さい値でも安定動作

#### Test Case 3-2: get_players - limit境界値（最大値1000）
🟡 **黄信号**: 要件定義書のlimit範囲1-1000仕様とパフォーマンス要件から推測

- **テスト名**: limit最大値での動作確認
  - **境界値の意味**: システムが一度に処理可能な最大取得件数
  - **境界値での動作保証**: パフォーマンス要件（1秒以内）を維持
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(1000),
      offset: Some(0),
      sort_by: Some("name".to_string()),
      sort_order: Some("asc".to_string()),
  }
  ```
  - **境界値選択の根拠**: limit範囲の最大値1000でのパフォーマンステスト
  - **実際の使用場面**: 大量データエクスポート、管理画面での一括表示
- **期待される結果**: 最大1000件のプレイヤーデータを1秒以内で返却
  - **境界での正確性**: 正確に1000件まで取得、レスポンス時間測定
  - **一貫した動作**: 小さいlimitと同じレスポンス構造
- **テストの目的**: 最大limit値でのパフォーマンス保証
  - **堅牢性の確認**: 大量データでもNFR-001要件（1秒以内）を満たす

#### Test Case 3-3: get_players - offset境界値（0）
🟡 **黄信号**: 一般的なページネーション仕様から推測した境界値テスト

- **テスト名**: offset最小値（0）での動作確認
  - **境界値の意味**: ページネーションの開始位置
  - **境界値での動作保証**: 最初のページが正確に取得される
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(10),
      offset: Some(0),
      sort_by: Some("name".to_string()),
      sort_order: Some("asc".to_string()),
  }
  ```
  - **境界値選択の根拠**: offset最小値での先頭データ取得確認
  - **実際の使用場面**: 最初のページ表示、データリセット後の表示
- **期待される結果**: 最初の10件のプレイヤーデータ
  - **境界での正確性**: 正確に最初の10件を取得
  - **一貫した動作**: オフセット指定時と同じレスポンス構造
- **テストの目的**: offset最小値での動作保証
  - **堅牢性の確認**: 境界値でのページネーション精度

#### Test Case 3-4: get_players - 大きなoffset値（データ件数超過）
🟡 **黄信号**: 実用的な境界値テストとして大きなoffset値での動作を推測

- **テスト名**: データ件数を超えるoffset指定
  - **境界値の意味**: 存在するデータ範囲を超えたoffset指定
  - **境界値での動作保証**: 空の結果でもエラーにならない
- **入力値**:
  ```rust
  GetPlayersRequest {
      limit: Some(10),
      offset: Some(1000000), // 非常に大きなoffset
      sort_by: Some("name".to_string()),
      sort_order: Some("asc".to_string()),
  }
  ```
  - **境界値選択の根拠**: データ範囲を大幅に超えるoffset値でのテスト
  - **実際の使用場面**: ページング計算ミス、古いブックマーク
- **期待される結果**: 空のプレイヤー配列、total_countは正確、has_more=false
  - **境界での正確性**: エラーにならず空結果を返す
  - **一貫した動作**: レスポンス構造は通常と同じ
- **テストの目的**: 範囲外offset値でのエラーハンドリング確認
  - **堅牢性の確認**: 極端な値でもサーバーエラーにならない

#### Test Case 3-5: update_player - プレイヤー名最小長（1文字）
🔵 **青信号**: EDGE-101（1文字プレイヤー名対応）要件に明記

- **テスト名**: プレイヤー名最小文字数での更新
  - **境界値の意味**: システムが受け入れ可能な最短プレイヤー名
  - **境界値での動作保証**: 1文字の名前でも正常処理される
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: Some("A".to_string()), // 1文字
      player_type_id: None,
  }
  ```
  - **境界値選択の根拠**: EDGE-101要件で1文字対応が明記されている
  - **実際の使用場面**: イニシャルのみのプレイヤー、海外プレイヤーの短い名前
- **期待される結果**: 1文字の名前で正常に更新されたPlayerオブジェクト
  - **境界での正確性**: 1文字でもバリデーションをパス、表示も正常
  - **一貫した動作**: 長い名前と同じ更新処理フロー
- **テストの目的**: 最短プレイヤー名の受け入れ確認
  - **堅牢性の確認**: 極端に短い名前でも安定動作

#### Test Case 3-6: update_player - プレイヤー名最大長（255文字）
🔵 **青信号**: EDGE-102（255文字長名対応）要件に明記

- **テスト名**: プレイヤー名最大文字数での更新
  - **境界値の意味**: システムが格納可能な最長プレイヤー名
  - **境界値での動作保証**: 255文字の名前でも正常処理され、表示される
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: Some("A".repeat(255)), // 255文字
      player_type_id: None,
  }
  ```
  - **境界値選択の根拠**: EDGE-102要件で255文字対応が明記されている
  - **実際の使用場面**: 極端に長いユーザー名、フルネーム＋追加情報
- **期待される結果**: 255文字の名前で正常に更新、UI表示も適切
  - **境界での正確性**: 255文字すべてが保存・取得される
  - **一貫した動作**: 文字数に関わらず同じ処理フロー
- **テストの目的**: 最長プレイヤー名の処理確認
  - **堅牢性の確認**: 長い名前でもパフォーマンス劣化なし

#### Test Case 3-7: update_player - プレイヤー名超過長（256文字）
🟡 **黄信号**: EDGE-102要件の境界値を超えた場合のエラーハンドリングを推測

- **テスト名**: プレイヤー名文字数制限超過エラー
  - **境界値の意味**: システムの文字数制限を1文字超過した場合
  - **境界値での動作保証**: 適切なエラーメッセージでバリデーション失敗
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: Some("A".repeat(256)), // 256文字（制限超過）
      player_type_id: None,
  }
  ```
  - **境界値選択の根拠**: 255文字制限を1文字超過した境界値テスト
  - **実際の発生シナリオ**: ユーザーの極端な入力、インポート時のデータエラー
- **期待される結果**: `Err("プレイヤー名は255文字以下である必要があります")`
  - **境界での正確性**: 制限値を超えた場合の明確なエラー
  - **一貫した動作**: バリデーション統一ルールの適用
- **テストの目的**: 文字数制限バリデーションの確認
  - **堅牢性の確認**: 制限超過時の適切なエラーハンドリング

#### Test Case 3-8: update_player - プレイヤータイプ削除（None指定）
🔵 **青信号**: types.rsのUpdatePlayerRequest型定義（Option<Option<String>>）に基づく

- **テスト名**: プレイヤータイプ削除（NULL設定）
  - **境界値の意味**: プレイヤータイプを「なし」状態に設定する特殊ケース
  - **境界値での動作保証**: Some(None)指定でタイプが正しく削除される
- **入力値**:
  ```rust
  UpdatePlayerRequest {
      id: "existing_player_id".to_string(),
      name: None,
      player_type_id: Some(None), // タイプ削除を明示
  }
  ```
  - **境界値選択の根拠**: Option<Option<String>>型の特殊な使用方法
  - **実際の使用場面**: プレイヤー分類を取り消したい場合
- **期待される結果**: player_type_idがNULLに設定されたPlayerオブジェクト
  - **境界での正確性**: データベースでNULL値が正しく設定される
  - **一貫した動作**: UI表示でタイプなし状態が適切に表現される
- **テストの目的**: プレイヤータイプ削除機能の動作確認
  - **堅牢性の確認**: NULL状態での処理が安定動作

### 4. パフォーマンステストケース

#### Test Case 4-1: get_players - 大量データパフォーマンス
🔵 **青信号**: NFR-001（1秒以内表示）要件に基づく

- **テスト名**: 1000件データでのパフォーマンステスト
  - **何をテストするか**: NFR-001要件（1秒以内）の達成確認
  - **期待される動作**: 1000件のプレイヤーデータ存在時も1秒以内で応答
- **テスト設定**:
  - プレイヤーデータ1000件を事前挿入
  - limit=50でのデフォルト取得時間測定
- **期待される結果**: レスポンス時間 ≤ 1000ms
- **テストの目的**: パフォーマンス要件の達成確認
  - **品質保証の観点**: 実用的な大量データでの性能保証

#### Test Case 4-2: update_player - 同時更新パフォーマンス
🟡 **黄信号**: 一般的なデータベース性能要件から推測

- **テスト名**: 複数プレイヤー同時更新のパフォーマンス
  - **何をテストするか**: 複数の更新処理が同時実行された場合の性能
  - **期待される動作**: 各更新処理が200ms以内で完了
- **テスト設定**:
  - 10件のプレイヤーデータに対して同時に更新リクエスト
  - 各更新処理の実行時間測定
- **期待される結果**: 各更新処理が200ms以内で完了
- **テストの目的**: 同時アクセス時の性能確認
  - **品質保証の観点**: マルチユーザー環境での性能保証

## テストケース実装時の日本語コメント指針

🟡 **黄信号**: 既存テストパターンに日本語コメントが少ないため、品質向上のために追加

各テストケース実装時には以下の日本語コメントを必ず含める：

### 基本コメント構造
```rust
#[tokio::test]
async fn test_get_players_default_parameters() {
    // 【テスト目的】: get_playersコマンドがデフォルトパラメータで正常動作することを確認
    // 【テスト内容】: limit/offset/sort系をすべてNoneで指定し、デフォルト値での処理を検証
    // 【期待される動作】: limit=50, offset=0, sort_by="name", sort_order="asc"で処理される
    // 🔵 既存のget_all_players実装パターンと要件定義書に基づく確実なテスト

    // 【テストデータ準備】: 複数のプレイヤーデータを作成してソート動作を確認
    let db = Database::new_test().expect("テストデータベース作成失敗");

    // 【初期条件設定】: 異なる名前のプレイヤーを複数作成
    let players = vec!["Charlie", "Alice", "Bob"];
    for name in players {
        insert_test_player(&db, name);
    }

    // 【実際の処理実行】: get_playersコマンドをデフォルトパラメータで呼び出し
    let request = GetPlayersRequest {
        limit: None,
        offset: None,
        sort_by: None,
        sort_order: None,
    };
    let result = get_players(tauri::State(&db), request).await;

    // 【結果検証】: 取得結果が期待されるデフォルト動作と一致することを確認
    assert!(result.is_ok(), "正常系でエラーが発生"); // 【確認内容】: 成功応答の確認

    let response = result.unwrap();
    assert_eq!(response.players.len(), 3); // 【確認内容】: 全プレイヤーが取得されることを確認
    assert_eq!(response.players[0].name, "Alice"); // 【確認内容】: 名前昇順ソートの確認
    // 🔵 デフォルトソートがname昇順であることの確認
}
```

### セットアップ・クリーンアップのコメント
```rust
#[cfg(test)]
mod tests {
    use super::*;

    // 【テストヘルパー関数】: プレイヤーデータの簡単挿入
    async fn insert_test_player(db: &Database, name: &str) -> String {
        // 【テストデータ作成】: テスト用プレイヤーの標準作成処理
        let request = CreatePlayerRequest {
            name: name.to_string(),
            player_type_id: None,
        };
        let result = create_player(tauri::State(db), request).await;
        result.expect("テストプレイヤー作成失敗").id
    }

    // 【テストヘルパー関数】: プレイヤータイプの作成
    async fn insert_test_player_type(db: &Database, name: &str, color: &str) -> String {
        // 【テストデータ作成】: テスト用プレイヤータイプの標準作成処理
        let request = CreatePlayerTypeRequest {
            name: name.to_string(),
            color: color.to_string(),
        };
        let result = create_player_type(tauri::State(db), request).await;
        result.expect("テストプレイヤータイプ作成失敗").id
    }
}
```

## 実装必要な型定義

🟡 **黄信号**: 要件定義書に記載されているが、実際のtypes.rsに存在しない型を特定

TASK-0504実装のために以下の型定義を追加する必要があります：

```rust
// get_playersコマンド用リクエスト型
#[derive(Debug, Serialize, Deserialize)]
pub struct GetPlayersRequest {
    pub limit: Option<i32>,        // デフォルト50, 範囲1-1000
    pub offset: Option<i32>,       // デフォルト0, 範囲0以上
    pub sort_by: Option<String>,   // "name", "created_at", "updated_at"
    pub sort_order: Option<String>, // "asc", "desc", デフォルト"asc"
}

// get_playersコマンド用レスポンス型
#[derive(Debug, Serialize, Deserialize)]
pub struct GetPlayersResponse {
    pub players: Vec<PlayerListResponse>,
    pub total_count: i32,
    pub has_more: bool,
}
```

## 品質判定結果: ✅ 高品質

- **テストケース分類**: ✅ 正常系・異常系・境界値が網羅されている（28テストケース）
- **期待値定義**: ✅ 各テストケースの期待値が明確に定義されている
- **技術選択**: ✅ プログラミング言語（Rust）・テストフレームワーク（cargo test）が確定
- **実装可能性**: ✅ 現在の技術スタックで実現可能（既存17件のテスト実績）

### テストケース分類詳細
- **正常系**: 6テストケース（基本動作、ページネーション、ソート、部分更新など）
- **異常系**: 8テストケース（バリデーションエラー、参照整合性エラーなど）
- **境界値**: 8テストケース（最小・最大値、文字数制限、特殊ケースなど）
- **パフォーマンス**: 2テストケース（大量データ、同時更新性能）
- **実装サポート**: 4テストケース（ヘルパー関数、型定義など）

**合計**: 28テストケース
