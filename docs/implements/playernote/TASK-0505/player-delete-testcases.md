# TASK-0505: プレイヤー削除とカスケード処理 - テストケース洗い出し

## 生成情報
- **生成日**: 2025-09-25
- **生成者**: TDD テスト設計担当者
- **タスクID**: TASK-0505
- **機能名**: プレイヤー削除とカスケード処理（delete_player）
- **実装対象**: delete_player コマンド

## 開発言語・フレームワーク

🔵 **青信号**: TASK-0504実装実績と技術スタック定義書に基づく確実な選択

- **プログラミング言語**: Rust
  - **言語選択の理由**: Tauri バックエンドの実装言語として既に採用済み、TASK-0504で12テスト成功実績あり
  - **テストに適した機能**: cargo test標準フレームワーク、Result型によるエラーハンドリング、型安全性、トランザクション処理
- **テストフレームワーク**: cargo test + tokio-test
  - **フレームワーク選択の理由**: 既存プロジェクトで26テストが正常動作、非同期処理対応、SQLiteトランザクション対応
  - **テスト実行環境**: インメモリSQLite、Database::new_test()ヘルパー関数活用、カスケード削除対応

## テストケース一覧（18テストケース）

### 1. 正常系テストケース（基本的な動作）

#### Test Case 1-1: delete_player - 関連データありプレイヤーの削除
🔵 **青信号**: 要件定義書のケース1とDeletePlayerResponse型定義に基づく

- **テスト名**: プレイヤー削除（関連データあり）
  - **何をテストするか**: プレイヤータグ・メモが存在する場合のカスケード削除動作
  - **期待される動作**: プレイヤーと関連データがすべて削除され、削除結果が正確に報告される
- **入力値**:
  ```rust
  player_id: "test_player_with_data"
  ```
  - **入力データの意味**: 3つのタグと1つのメモを持つテスト用プレイヤー
- **期待される結果**:
  ```rust
  Ok(DeletePlayerResponse {
      deleted_player_id: "test_player_with_data",
      deleted_tags_count: 3,
      deleted_note: true,
  })
  ```
  - **期待結果の理由**: REQ-401（カスケード削除）要件に基づく完全削除
- **テストの目的**: カスケード削除機能の基本動作確認
  - **確認ポイント**: プレイヤーテーブルから削除、player_tagsから3件削除、player_notesから1件削除

#### Test Case 1-2: delete_player - 関連データなしプレイヤーの削除
🔵 **青信号**: 要件定義書のケース2とDeletePlayerResponse型定義に基づく

- **テスト名**: プレイヤー削除（関連データなし）
  - **何をテストするか**: タグ・メモが存在しない場合の削除動作
  - **期待される動作**: プレイヤーのみが削除され、関連データカウントが0で報告される
- **入力値**:
  ```rust
  player_id: "test_player_no_data"
  ```
  - **入力データの意味**: タグもメモも持たない最小構成のテスト用プレイヤー
- **期待される結果**:
  ```rust
  Ok(DeletePlayerResponse {
      deleted_player_id: "test_player_no_data",
      deleted_tags_count: 0,
      deleted_note: false,
  })
  ```
  - **期待結果の理由**: 関連データがない場合でも正常動作する必要がある
- **テストの目的**: 関連データなしケースでの正常動作確認
  - **確認ポイント**: プレイヤーのみ削除、関連テーブルへの影響なし

#### Test Case 1-3: delete_player - プレイヤータイプ設定ありプレイヤーの削除
🔵 **青信号**: データベース設計のFOREIGN KEY制約（ON DELETE SET NULL）に基づく

- **テスト名**: プレイヤー削除（プレイヤータイプ設定あり）
  - **何をテストするか**: player_type_idが設定されているプレイヤーの削除動作
  - **期待される動作**: プレイヤーが削除され、プレイヤータイプは削除されない（参照のみ削除）
- **入力値**:
  ```rust
  player_id: "test_player_with_type"
  ```
  - **入力データの意味**: アグレッシブタイプが設定されたテスト用プレイヤー
- **期待される結果**:
  ```rust
  Ok(DeletePlayerResponse {
      deleted_player_id: "test_player_with_type",
      deleted_tags_count: 0,
      deleted_note: false,
  })
  ```
  - **期待結果の理由**: プレイヤータイプはマスタデータなので削除されない
- **テストの目的**: プレイヤータイプとの関係性での正常動作確認
  - **確認ポイント**: プレイヤーのみ削除、player_typesテーブルは影響なし

### 2. 異常系テストケース（エラーハンドリング）

#### Test Case 2-1: delete_player - 存在しないプレイヤーID
🔵 **青信号**: 要件定義書のEDGE-501エラーケース定義に基づく

- **テスト名**: 存在しないプレイヤー削除エラー
  - **エラーケースの概要**: データベースに存在しないIDでの削除試行
  - **エラー処理の重要性**: 不正な削除要求に対する適切なエラーレスポンス
- **入力値**:
  ```rust
  player_id: "non_existent_player_id"
  ```
  - **不正な理由**: データベースに該当するプレイヤーが存在しない
  - **実際の発生シナリオ**: 削除後の重複削除、URLパラメータ改ざん、データ競合状態
- **期待される結果**:
  ```rust
  Err("指定されたプレイヤーが見つかりません")
  ```
  - **エラーメッセージの内容**: ユーザーにとって分かりやすい日本語メッセージ
  - **システムの安全性**: 不正な削除要求でもシステムエラーにならない
- **テストの目的**: 存在しないIDに対するエラーハンドリング確認
  - **品質保証の観点**: フロントエンドでの不正操作からシステムを保護

#### Test Case 2-2: delete_player - 空ID指定エラー
🔵 **青信号**: 要件定義書のEDGE-502エラーケース定義に基づく

- **テスト名**: 空プレイヤーID指定エラー
  - **エラーケースの概要**: 空文字列IDでの削除試行
  - **エラー処理の重要性**: 必須パラメータの適切なバリデーション
- **入力値**:
  ```rust
  player_id: ""
  ```
  - **不正な理由**: プレイヤーIDは必須項目であり空文字列は不正
  - **実際の発生シナリオ**: フロントエンドのバグ、APIの不正呼び出し
- **期待される結果**:
  ```rust
  Err("プレイヤーIDは必須です")
  ```
  - **エラーメッセージの内容**: 入力値の問題を明確に示す
  - **システムの安全性**: バリデーションエラーによる早期終了
- **テストの目的**: 入力バリデーションの確認
  - **品質保証の観点**: APIの堅牢性とデータ整合性保護

#### Test Case 2-3: delete_player - null/whitespace ID エラー
🟡 **黄信号**: EDGE-502の拡張として空白文字・null相当の処理を推測

- **テスト名**: null・空白文字ID指定エラー
  - **エラーケースの概要**: null相当値や空白文字のみのIDでの削除試行
  - **エラー処理の重要性**: さまざまな不正入力パターンへの対応
- **入力値**:
  ```rust
  player_id: "   " // 空白文字のみ
  ```
  - **不正な理由**: トリムすると空文字列になるため、実質的に空ID
  - **実際の発生シナリオ**: フォーム入力での空白入力、データ変換エラー
- **期待される結果**:
  ```rust
  Err("プレイヤーIDは必須です")
  ```
  - **エラーメッセージの内容**: 空IDと同じエラーメッセージで一貫性保持
  - **システムの安全性**: 空白文字も適切にバリデーション
- **テストの目的**: 入力バリデーションの包括性確認
  - **品質保証の観点**: さまざまな不正入力への統一的な対応

### 3. 境界値テストケース（最小値、最大値、大量データ等）

#### Test Case 3-1: delete_player - 最大文字数ID
🟡 **黄信号**: 入力制約（最大255文字）から境界値での動作を推測

- **テスト名**: 最大文字数プレイヤーID削除
  - **境界値の意味**: ID長さ制限の境界値での動作確認
  - **境界値での動作保証**: 長いIDでも正常削除される必要がある
- **入力値**:
  ```rust
  player_id: "a".repeat(255) // 255文字のID
  ```
  - **境界値選択の根拠**: 要件定義の最大長255文字制限
  - **実際の使用場面**: 極端に長いID生成時の動作確認
- **期待される結果**:
  ```rust
  Err("指定されたプレイヤーが見つかりません") // 通常は存在しないため
  ```
  - **境界での正確性**: 長いIDでも適切にクエリ実行される
  - **一貫した動作**: ID長に関係なく同じ削除ロジックが動作
- **テストの目的**: ID長さ境界でのシステム安定性確認
  - **堅牢性の確認**: 極端な入力値でもシステムエラーにならない

#### Test Case 3-2: delete_player - 大量関連データの削除
🔵 **青信号**: 要件定義書のEDGE-503（大量データ削除）とNFR-001（1秒以内）に基づく

- **テスト名**: 大量関連データプレイヤー削除
  - **境界値の意味**: 大量の関連データがある場合の性能と正確性
  - **境界値での動作保証**: 大量データでも1秒以内に完全削除される
- **入力値**:
  ```rust
  player_id: "test_player_heavy_data" // 100個のタグ関連を持つ
  ```
  - **境界値選択の根拠**: 要件定義の性能要件（1000件でも1秒以内）
  - **実際の使用場面**: ヘビーユーザーのデータ削除時の性能確認
- **期待される結果**:
  ```rust
  Ok(DeletePlayerResponse {
      deleted_player_id: "test_player_heavy_data",
      deleted_tags_count: 100,
      deleted_note: true,
  })
  ```
  - **境界での正確性**: 大量データでも削除カウントが正確
  - **一貫した動作**: データ量に関係なく同じ削除品質
- **テストの目的**: 大量データでの性能要件確認
  - **堅牢性の確認**: スケーラビリティとパフォーマンス保証

#### Test Case 3-3: delete_player - 特殊文字を含むID
🟡 **黄信号**: ID形式（16進数文字列）から特殊文字での動作を推測

- **テスト名**: 特殊文字ID削除
  - **境界値の意味**: ID形式の境界（16進数以外の文字）での動作
  - **境界値での動作保証**: 不正な形式でも適切にエラーハンドリング
- **入力値**:
  ```rust
  player_id: "player-123-!@#$%^&*()" // 特殊文字を含むID
  ```
  - **境界値選択の根拠**: 16進数文字列形式の境界外文字
  - **実際の使用場面**: 不正なAPI呼び出し、データ破損時の対応
- **期待される結果**:
  ```rust
  Err("指定されたプレイヤーが見つかりません") // 存在しないため
  ```
  - **境界での正確性**: 不正形式でも適切に処理される
  - **一貫した動作**: ID形式に関係なく同じ検索ロジック
- **テストの目的**: ID形式境界での安定性確認
  - **堅牢性の確認**: 不正形式入力でもシステムが異常終了しない

### 4. トランザクション・パフォーマンステストケース

#### Test Case 4-1: delete_player - トランザクション整合性
🟡 **黄信号**: データベーストランザクション要件から原子性動作を推測

- **テスト名**: トランザクション整合性確認
  - **何をテストするか**: カスケード削除の原子性（全削除か全ロールバック）
  - **期待される動作**: 部分削除状態にならず、完全削除かエラー時全ロールバック
- **入力値**:
  ```rust
  player_id: "test_player_transaction" // トランザクションテスト用
  ```
  - **入力データの意味**: 意図的にエラーを発生させるトランザクション境界テスト
- **期待される結果**:
  ```rust
  // 成功時：全データ削除
  // 失敗時：全データ変更なし（原子性保証）
  ```
  - **期待結果の理由**: データ整合性を保つトランザクション処理
- **テストの目的**: データベーストランザクションの原子性確認
  - **確認ポイント**: 中途半端な削除状態にならない

#### Test Case 4-2: delete_player - 削除性能測定
🔵 **青信号**: NFR-001（1秒以内応答）要件に基づく

- **テスト名**: 削除処理性能確認
  - **何をテストするか**: 削除処理が1秒以内に完了するかの性能測定
  - **期待される動作**: どのようなデータ量でも1秒以内に処理完了
- **入力値**:
  ```rust
  player_id: "test_player_performance" // 中程度のデータ量
  ```
  - **入力データの意味**: 実運用で想定される標準的なデータ量を持つプレイヤー
- **期待される結果**:
  ```rust
  // 処理時間 < 1秒
  // 削除成功
  ```
  - **期待結果の理由**: NFR-001パフォーマンス要件の保証
- **テストの目的**: パフォーマンス要件の定量的確認
  - **確認ポイント**: 実行時間の測定、メモリ使用量の確認

#### Test Case 4-3: delete_player - 同時削除リクエスト
🟡 **黄信号**: 並行アクセス時の動作を推測

- **テスト名**: 同時削除リクエスト処理
  - **何をテストするか**: 同じプレイヤーに対する同時削除要求の処理
  - **期待される動作**: 最初の削除が成功し、2番目は適切なエラー
- **入力値**:
  ```rust
  player_id: "test_player_concurrent" // 同時削除テスト用
  ```
  - **入力データの意味**: 複数のクライアントが同時に削除要求するシナリオ
- **期待される結果**:
  ```rust
  // 1st request: Ok(DeletePlayerResponse)
  // 2nd request: Err("指定されたプレイヤーが見つかりません")
  ```
  - **期待結果の理由**: データベースレベルでの一意性制約保護
- **テストの目的**: 並行処理での安全性確認
  - **確認ポイント**: 競合状態でのデータ整合性保持

## テストケース実装時の日本語コメント指針

### テストケース開始時のコメント

```rust
#[tokio::test]
async fn test_delete_player_with_related_data() {
    // 【テスト目的】: プレイヤー削除時のカスケード削除機能が正常動作することを確認
    // 【テスト内容】: タグ・メモを持つプレイヤーの削除で関連データもすべて削除される
    // 【期待される動作】: DeletePlayerResponseで正確な削除結果が報告される
    // 🔵 青信号: 要件定義書のケース1とDeletePlayerResponse定義に基づく
}
```

### Given（準備フェーズ）のコメント

```rust
// 【テストデータ準備】: カスケード削除を確認するため、プレイヤーに関連データを設定
let db = create_test_database();

// 【初期条件設定】: プレイヤー・タグ・メモの関連データを作成
let player_id = create_test_player(&db, "Test Player", None);
let _tag1 = create_test_player_tag(&db, &player_id, "tag1", 5);
let _tag2 = create_test_player_tag(&db, &player_id, "tag2", 7);
let _tag3 = create_test_player_tag(&db, &player_id, "tag3", 3);
let _note = create_test_player_note(&db, &player_id, "Test note content");

// 【前提条件確認】: テストデータが正しく作成されていることを確認
let initial_tags = get_player_tags_count(&db, &player_id);
assert_eq!(initial_tags, 3, "プレイヤータグが3件作成されている");
```

### When（実行フェーズ）のコメント

```rust
// 【実際の処理実行】: delete_playerコマンドを呼び出してプレイヤーを削除
let result = delete_player(db.clone(), player_id.clone()).await;

// 【処理内容】: カスケード削除によりプレイヤーとすべての関連データを削除
// 【実行タイミング】: テストデータ作成直後に削除を実行（データの新鮮性確保）
```

### Then（検証フェーズ）のコメント

```rust
// 【結果検証】: 削除処理が成功し、適切なレスポンスが返されることを確認
assert!(result.is_ok(), "削除処理が成功する");
let response = result.unwrap();

// 【期待値確認】: 削除されたデータの詳細が正確に報告される
// 🔵 青信号: DeletePlayerResponse型定義に基づく検証項目
assert_eq!(response.deleted_player_id, player_id, "削除されたプレイヤーIDが正確");
assert_eq!(response.deleted_tags_count, 3, "削除されたタグ数が正確");
assert_eq!(response.deleted_note, true, "メモが削除されたことが報告される");

// 【品質保証】: データベースから実際にデータが削除されていることを直接確認
let remaining_player = get_player_by_id(&db, &player_id);
assert!(remaining_player.is_none(), "プレイヤーがデータベースから削除されている");

let remaining_tags = get_player_tags_count(&db, &player_id);
assert_eq!(remaining_tags, 0, "関連タグがすべて削除されている");

let remaining_note = get_player_note(&db, &player_id);
assert!(remaining_note.is_none(), "関連メモが削除されている");
```

### 個別検証項目のコメント

```rust
// 【検証項目】: カスケード削除による関連データの完全削除を確認
// 🔵 青信号: データベース設計のFOREIGN KEY (ON DELETE CASCADE)制約に基づく
assert_eq!(response.deleted_tags_count, expected_tag_count);

// 【検証項目】: プレイヤータイプマスタデータが削除されていないことを確認
// 🔵 青信号: プレイヤータイプはマスタデータのため削除対象外の設計に基づく
let player_types_count = get_player_types_count(&db);
assert_eq!(player_types_count, initial_player_types_count);
```

### セットアップ・クリーンアップのコメント

```rust
// 【テスト前準備】: 各テストで独立したデータベース環境を作成
beforeEach(() => {
    // 【環境初期化】: インメモリSQLiteで高速なテスト実行環境を提供
    let db = Database::new_test().expect("テストデータベース作成成功");
});

// 【テスト後処理】: インメモリデータベースのため自動的にクリーンアップ
afterEach(() => {
    // 【状態復元】: 各テストが独立実行されるため特別なクリーンアップ不要
    // インメモリデータベースがスコープ終了で自動削除される
});
```

## 品質判定

### ✅ **高品質**
- **テストケース分類**: 正常系（3件）・異常系（3件）・境界値（3件）・性能（3件）で網羅
- **期待値定義**: 各テストケースでDeletePlayerResponse構造体での明確な期待値定義
- **技術選択**: Rust + cargo test（TASK-0504で実証済み）、インメモリSQLite、非同期処理対応
- **実装可能性**: 既存のDatabase::new_test()とヘルパー関数活用で確実に実現可能

## 次のステップ

**次のお勧めステップ: `/tdd-red` でRedフェーズ（失敗テスト作成）を開始します。**

- 18テストケースの実装（正常系・異常系・境界値・性能）
- カスケード削除の動作確認
- トランザクション整合性の検証
- パフォーマンス要件（1秒以内）の測定実装